# 部署说明

这个文件夹是可直接部署的静态网站。

最快分享方式：

1. 打开 https://app.netlify.com/drop
2. 上传整个 `site` 文件夹，或上传外层目录里的 `site.zip`
3. 复制生成的网址分享

GitHub Pages：

1. 新建公开仓库
2. 上传本文件夹内所有内容到仓库根目录
3. 在 `Settings` -> `Pages` 里选择从 `main` 分支根目录部署

注意：必须保留 `lhasa_real_maps` 文件夹，路线导航折线和图片都依赖它。
